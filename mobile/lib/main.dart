// NubeRush Driver — application entry point.
//
// Dr.1.3.B skeleton: this file only bootstraps the app. It deliberately
// contains no API calls, no Supabase auth, and no driver business logic.
// Those arrive in later Dr.1.3 subphases (C: API client, D: auth,
// E+: driver surfaces).

import 'package:flutter/widgets.dart';

import 'app/app.dart';

void main() {
  runApp(const NubeRushDriverApp());
}
